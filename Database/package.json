{
  "name": "database",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "database": "psql -U postgres -d postgres -f create_database.sql",
    "tables": "psql -U raybyam -d sheet -f create_tables.sql",
    "functions": "psql -U raybyam -d sheet -f create_functions.sql",
    "data": "psql -U raybyam -d sheet -f create_mock_data.sql"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
